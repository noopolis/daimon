# Core Guide

`src/core` contains Daimon’s runtime-neutral contracts and small pure helpers. Keep filesystem discovery at the boundary and make evaluators injectable and deterministic. Tests belong beside the implementation.
